#include<bits/stdc++.h>

#define fore(i,a,b) for(int i = a; i< (int)b;i++)
#define FAST_IO ios::sync_with_stdio(false),cin.tie(0),cout.tie(0);
using namespace std;

typedef long long ll;



int main(){
    FAST_IO
    int n; vector<int> nums(n);
    cin >> n; 
    fore(i,0,n) cin >> nums[i];
    
}


